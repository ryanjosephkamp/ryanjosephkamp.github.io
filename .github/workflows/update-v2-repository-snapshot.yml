name: Update repository snapshot

on:
  workflow_dispatch:
  schedule:
    - cron: "17 8 * * *"

permissions:
  contents: write

jobs:
  update-repository-snapshot:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Refresh public repository snapshot
        run: npm run refresh:repos

      - name: Commit updated snapshot
        run: |
          if git diff --quiet -- repositories/data/repos.snapshot.json; then
            echo "No repository snapshot changes."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add repositories/data/repos.snapshot.json
          git commit -m "Update repository snapshot"
          git push
